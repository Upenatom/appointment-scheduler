<%- include('../partials/header') %>
<h1 class="maintitle">Your Vehicles</h1>
<%if(!customer.firstName){%>
    <h2>Profile Setup</h2>
    <p>Please fill out the form to set up your account:</p>
    <form action="/cars/newcustomer?_method=PUT" method="POST" autocomplete="'off">
    <p>E-mail:</p>
    <p><%=customer.email%></p>
    <p>Fist Name:</p>
    <input type="text" name="firstName">
    <p>Last Name:</p>
    <input type="text" name="lastName">
    <p>Phone #:</p>
    <input type="text" name="phone" >
    <p>Make:</p>
    <input type="text" name="make">
    <p>Model:</p>
    <input type="text" name="model">
    <p>Year:</p>
    <input type="text" name="year">
    <button type="submit">Submit</button>
   </form>
    <% } else{%> 
        <%if (customer.isAdmin){%>
            <a class="admin" href="/admin">ADMIN</a>
        <%}%>
        <h2> Welcome <%= customer.firstName%> </h2>

       <div><p>Click a Vehicle Name to view/book appointments</p></div>
        <ul type="none">
        <% car.forEach(function(c){%>


        <li>
            <form method="POST" action="/cars/<%=c.id%>?_method=DELETE"><button type="submit" ><i class="material-icons">delete_forever</i></button></form>

            <a href="/cars/<%=c.id%>"><%= c.make %> <%= c.model %> <%=c.year %></a>
            
            <form action="/cars/<%=c.id%>/edit" method="GET"><button type="submit" ><i class="material-icons">build</i></button></form>
           
        </li> 
        <%})%>
        </ul>
        <form action="/cars/new/">
            <button type="submit"> <i class="material-icons">add</i>
            Add a new Vehicle</button>
        </form>
        <% } %>
        
<%- include('../partials/footer') %>